pipeline {
    agent any
    stages {
        stage('Run Python Script') {
            steps {
                script {
                    
                    // 运行Python脚本
                    bat 'MyJenkinsTest\\name.py'
                }
            }
        }
    }
}
